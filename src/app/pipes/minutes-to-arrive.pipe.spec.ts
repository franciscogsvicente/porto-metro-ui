import { MinutesToArrivePipe } from './minutes-to-arrive.pipe';

describe('MinutesToArrivePipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesToArrivePipe();
    expect(pipe).toBeTruthy();
  });
});
